# --- Model and Preprocessor Loading ---
# Paths are relative to the container's /app directory, where the volumes are mounted.
MODEL_PATH = '/app/trained-models/mlp_malicious_traffic_model.joblib'
PREPROCESSOR_PATH = '/app/trained-models/mlp_malicious_traffic_preprocessor.joblib'